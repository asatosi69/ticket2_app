<% if current_seller.admin_flag %>
  <h3>『取扱者』管理ページ</h3>
  <br>
  <p>『管理者フラグ』が立っているのが、『管理者』、立っていないのが『取扱者』です。</p>
  <p>ボタン『反転』をクリックすると、『管理者フラグ』を『ON/OFF』します。</p>
  <p>『管理者』は全部の操作が可能ですが、『取扱者』は一部の操作のみが可能です。</p>
  <br>
  <p>チェックボックスでレ点がついている状態で、ボタン『一括送信』を押下すると、『管理者』/『取扱者』に『予約用URL』がメールされます。</p>
<% else %>
  <h3>『取扱者』情報ページ</h3>
<% end %>
<br>
<%= form_tag(mail_all_path, method: 'get') do %>
  <div class="table-responsive">
    <table class="table table-striped table-bordered table-hover">
      <thead>
        <tr>
          <% if current_seller.admin_flag %>
            <th style="width: 60px" style="white-space:nowrap;"><div class="position-in-the-cell1"> </div></th>
            <th style="width: 60px" style="white-space:nowrap;"><div class="position-in-the-cell1">反転</div></th>
            <th style="width: 60px" style="white-space:nowrap;"><div class="position-in-the-cell1">削除</div></th>
          <% end %>
          <th style="width: 150px" style="white-space:nowrap;"><div class="position-in-the-cell1">取扱者</div></th>
          <th style="width: 120px" style="white-space:nowrap;"><div class="position-in-the-cell1">管理者フラグ</div></th>
          <th style="white-space:nowrap;"><div class="position-in-the-cell1">予約用URL</div></th>
        </tr>
      </thead>

      <tbody>
          <% @sellers.each do |seller| %>
            <tr>
              <% if current_seller.admin_flag %>
                <td style="white-space:nowrap;"><div class="position-in-the-cell2"><%= check_box_tag 'sellers[]',seller.id %></div></td>
                <td style="white-space:nowrap;"><div class="position-in-the-cell2"><%= link_to '反転', seller_path(seller), {method: "patch", class: 'btn btn-default'} %></div></td>
                <td style="white-space:nowrap;"><div class="position-in-the-cell2"><%= link_to '削除', seller_path(seller), {method: "delete", class: 'btn btn-default'} %></div></td>
              <% end %>
              <td style="white-space:nowrap;"><div class="position-in-the-cell2"><%= seller.name %></div></td>
              <td style="white-space:nowrap;"><div class="position-in-the-cell2">
                <% if seller.admin_flag %>
                  ☑️
                <% else %>
                  -
                <% end %>
              </div></td>
              <td style="white-space:nowrap;"><div class="position-in-the-cell2"><%= "#{@host_with_port}/registers/#{seller.id}/new" %></div></td>
            </tr>
            <% end %>
      </tbody>
    </table>
  </div>
  <% if current_seller.admin_flag %>
   <%= submit_tag "一括送信" %>
   <%= link_to :back, :style=>"color:#333333" do %>
     <button>戻る</button>
   <% end %>
  <% else %>
  <%= link_to :back, :style=>"color:#333333" do %>
    <button>戻る</button>
  <% end %>
  <% end %>
<% end %>
<%= paginate @sellers -%>
