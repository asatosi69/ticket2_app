<% if current_seller.admin_flag %>
  <h3>『取扱者』管理ページ</h3>
  <br>
  <p>『管理者フラグ』が立っているのが、『管理者』、立っていないのが『取扱者』です。</p>
  <p>ボタン『反転』をクリックすると、『管理者フラグ』を『ON/OFF』します。</p>
  <p>『管理者』は全部の操作が可能ですが、『取扱者』は一部の操作のみが可能です。</p>
  <br>
  <p>チェックボックスでレ点がついている状態で、ボタン『一括送信』を押下すると、『管理者』/『取扱者』に『予約用URL』がメールされます。</p>
<% else %>
  <h3>『取扱者』情報ページ</h3>
<% end %>
<br>
<%= form_tag(mail_all_path, method: 'get') do %>
  <div class="table-responsive">
    <table class="table table-striped table-bordered table-hover">
      <thead>
        <tr>
          <% if current_seller.admin_flag %>
            <th style="width: 70px"><p class="text-center"> </p></th>
            <th style="width: 70px"><p class="text-center">フラグ反転</p></th>
            <th style="width: 70px"><p class="text-center">削除</p></th>
          <% end %>
          <th style="width: 150px"><p class="text-center">取扱者</p></th>
          <th style="width: 120px"><p class="text-center">管理者フラグ</p></th>
          <th><p class="text-center">予約用URL</p></th>
        </tr>
      </thead>

      <tbody>
          <% @sellers.each do |seller| %>
            <tr>
              <% if current_seller.admin_flag %>
                <td class="align-middle"><p class="text-center"><%= check_box_tag 'sellers[]',seller.id %></p></td>
                <td class="align-middle"><%= link_to '反転', seller_path(seller), {method: "patch", class: 'btn btn-default'} %></td>
                <td class="align-middle"><%= link_to '削除', seller_path(seller), {method: "delete", class: 'btn btn-default'} %></td>
              <% end %>
              <td class="align-middle"><p class="text-center"><%= seller.name %></p></td>
              <td class="align-middle"><p class="text-center">
                <% if seller.admin_flag %>
                  ☑️
                <% else %>
                  -
                <% end %>
              </p></td>
              <td class="align-middle"><p class="text-center"><%= "#{@host_with_port}/registers/#{seller.id}/new" %></p></td>
            </tr>
            <% end %>
      </tbody>
    </table>
  </div>
  <% if current_seller.admin_flag %>
   <%= submit_tag "一括送信" %>
   <%= link_to :back, :style=>"color:#333333" do %>
     <button>戻る</button>
   <% end %>
  <% else %>
  <%= link_to :back, :style=>"color:#333333" do %>
    <button>戻る</button>
  <% end %>
  <% end %>
<% end %>
<%= paginate @sellers -%>
