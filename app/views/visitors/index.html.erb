<h3>『来場者一覧』</h3>
<br>
<p>表示したい『公演』と表示させたい『並び順』を選択し、『表示』を押下ください。</p>
<br>
<%= form_tag visitors_path, method: :get do %>
  <p>公演</p>
  <div class="form-group">
    <%= select_tag 'stage_id', options_from_collection_for_select(Stage.all, :id, :stage, params[:stage_id]),
   {prompt: "選択して下さい"} %>
  </div>
  <p>並び順</p>
  <div class="form-group">
    <%= select_tag 'order_id', options_for_select({ 'フリガナ（昇順）': :order_by_buyer_furigana , '取扱者（昇順）⇒フリガナ（昇順）': :order_by_seller_id_and_buyer_furigana }),  {prompt: "選択して下さい"} %>
  </div>
  <div class="form-group">
    <button>表示</button>
  </div>
<% end %>
<br>

<%= render :partial => "shared/book_for_visitors" %>
<br>
<br>
<%= render :partial => "shared/ticket_format_for_visitors" %>
